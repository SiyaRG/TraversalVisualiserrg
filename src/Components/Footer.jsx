import React from "react";
import { Container, Row, Col } from "react-bootstrap";
const GlobalFooter = () => {
	return (
		<footer className="bg-body-tertiary p-4 d-flex justify-content-center"></footer>
	);
};

export default GlobalFooter;
